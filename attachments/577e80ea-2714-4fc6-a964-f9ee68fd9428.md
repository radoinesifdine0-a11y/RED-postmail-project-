# Store Parcel API - Part 2: Write Endpoints

## Extension du Training - Endpoints d'Écriture CRUD

**Prérequis:** Avoir complété la Part 1 (storeparcelapi.md) avec P95 <60ms validé

**Objectif:** Étendre l'API en ajoutant les endpoints d'écriture avec validation, gestion d'erreurs avancée et tests unitaires/intégration.

---

## 1. État Actuel du Projet

### 1.1 Endpoints Existants

Le projet dispose déjà de ces endpoints :

| Method | Path | Description | Status |
|--------|------|-------------|--------|
| GET | `/store_parcels/health` | Health check | ✅ Existant |
| POST | `/store_parcels/search` | Recherche parcels | ✅ Existant |
| GET | `/store_parcels/parcel/{parcelId}` | Get parcel by ID | ✅ Existant |
| POST | `/store_parcels/create` | Créer un parcel | ✅ Existant |

### 1.2 Structure Existante

**Package:** `com.kantic.storeParcelsWS`

**Modèle Parcel (Entity):**
```java
@Document(collection = "parcels")
public class Parcel {
    @Id
    private String id;                          // MongoDB internal ID

    @Indexed(unique = true)
    private String parcelId;                    // Unique parcel identifier

    @Field("customerOrderId")
    private String orderId;                     // Maps to DB: customerOrderId

    @Field("deliveryStoreId")
    private String pickupStoreId;               // Maps to DB: deliveryStoreId

    @Field("parcelStatus")
    private ParcelStatus status;                // Enum

    @Field("parcelStatusUpdateDate")
    private Instant statusUpdateDate;

    @Field("expirationDateTime")
    private Instant expirationDate;

    private Boolean isExpired;
    private Boolean isPaid;

    private Customer customer;                  // Nested object

    @Field("storeParcelStorageLocation")
    private StorageLocation storageLocation;    // Nested object
}
```

**ParcelStatus Enum (existant):**
```java
public enum ParcelStatus {
    PENDING,
    IN_TRANSIT,
    READY_FOR_PICKUP,
    PICKED_UP,
    CANCELLED,
    RETURNED,
    EXPIRED,
    DELIVERED
}
```

---

## 2. Nouveaux Endpoints à Implémenter

### 2.1 Endpoint PUT - Mettre à jour un Parcel

**HTTP Method:** `PUT`
**Path:** `/store_parcels/parcel/{parcelId}`
**Content-Type:** `application/json`

**Request Schema (UpdateParcelRequestDTO à créer):**

```java
public record UpdateParcelRequestDTO(
    String parcelStatus,                        // Optional, enum value as string
    Boolean isPaid,                             // Optional
    Instant expirationDate,                     // Optional
    @Valid CustomerUpdateDTO customer,          // Optional
    @Valid StorageLocationUpdateDTO storageLocation  // Optional
) {
    public record CustomerUpdateDTO(
        String email,                           // Optional
        String phoneNumber                      // Optional
    ) {}

    public record StorageLocationUpdateDTO(
        String locationCode                     // Maps to storeStorageLocationCode
    ) {}
}
```

**Champs NON modifiables:**
- `parcelId`
- `customerOrderId` (orderId dans l'entity)
- `deliveryStoreId` (pickupStoreId dans l'entity)
- `customer.firstName`
- `customer.lastName`

**Business Rules:**
- `parcelStatusUpdateDate` mis à jour automatiquement si `parcelStatus` change
- `isExpired` recalculé si `expirationDate` change : `expirationDate.isBefore(Instant.now())`
- Transition de statut validée selon machine d'état (voir 2.3)

**Response (200 OK):** `ParcelResponseDTO` (existant)

```json
{
  "parcelId": "P000001",
  "customerOrderId": "ORD123456",
  "deliveryStoreId": "ST001",
  "parcelStatus": "DELIVERED",
  "isPaid": true,
  "customer": {
    "firstName": "John",
    "lastName": "Doe",
    "email": "john.doe@example.com"
  },
  "expirationDate": "2025-02-15T10:00:00Z",
  "isExpired": false
}
```

**Status Codes:**
- `200 OK` - Mise à jour réussie
- `400 Bad Request` - Validation échouée ou transition invalide
- `404 Not Found` - Parcel inexistant

---

### 2.2 Endpoint DELETE - Supprimer un Parcel (Soft Delete)

**HTTP Method:** `DELETE`
**Path:** `/store_parcels/parcel/{parcelId}`

**Business Rules:**
- Soft delete uniquement (ajouter un flag `deleted: Boolean` dans l'entity)
- Les parcels supprimés sont exclus des recherches
- Seuls les parcels avec status `CANCELLED` ou `RETURNED` peuvent être supprimés

**Response (204 No Content):** Aucun body

**Status Codes:**
- `204 No Content` - Suppression réussie
- `400 Bad Request` - Parcel non supprimable (status actif)
- `404 Not Found` - Parcel inexistant ou déjà supprimé

---

### 2.3 Machine d'État des Statuts

```
┌─────────────┐
│   PENDING   │
└──────┬──────┘
       │
       ▼
┌─────────────┐     ┌─────────────┐
│ IN_TRANSIT  │────►│  CANCELLED  │
└──────┬──────┘     └─────────────┘
       │                   ▲
       ▼                   │
┌─────────────────┐        │
│READY_FOR_PICKUP │────────┤
└───────┬─────────┘        │
        │                  │
   ┌────┼────┐             │
   ▼    ▼    ▼             │
┌────┐ ┌───────┐ ┌─────────┘
│PICK│ │EXPIRED│ │
│_UP │ └───┬───┘ │
└─┬──┘     │     │
  │        ▼     │
  │  ┌──────────┐│
  │  │ RETURNED ││
  │  └──────────┘│
  ▼              │
┌─────────────┐  │
│  DELIVERED  │◄─┘ (from PICKED_UP)
└─────────────┘
```

**Transitions autorisées (à implémenter dans ParcelStatus):**

| From | To (allowed) |
|------|--------------|
| `PENDING` | `IN_TRANSIT`, `CANCELLED` |
| `IN_TRANSIT` | `READY_FOR_PICKUP`, `CANCELLED` |
| `READY_FOR_PICKUP` | `PICKED_UP`, `EXPIRED`, `CANCELLED` |
| `PICKED_UP` | `DELIVERED`, `CANCELLED` |
| `EXPIRED` | `RETURNED`, `CANCELLED` |
| `DELIVERED` | (terminal) |
| `RETURNED` | (terminal) |
| `CANCELLED` | (terminal) |

**Implémentation à ajouter dans `ParcelStatus.java`:**

```java
public enum ParcelStatus {
    PENDING,
    IN_TRANSIT,
    READY_FOR_PICKUP,
    PICKED_UP,
    CANCELLED,
    RETURNED,
    EXPIRED,
    DELIVERED;

    private static final Map<ParcelStatus, Set<ParcelStatus>> ALLOWED_TRANSITIONS = Map.of(
        PENDING, Set.of(IN_TRANSIT, CANCELLED),
        IN_TRANSIT, Set.of(READY_FOR_PICKUP, CANCELLED),
        READY_FOR_PICKUP, Set.of(PICKED_UP, EXPIRED, CANCELLED),
        PICKED_UP, Set.of(DELIVERED, CANCELLED),
        EXPIRED, Set.of(RETURNED, CANCELLED)
        // DELIVERED, RETURNED, CANCELLED are terminal states
    );

    public boolean canTransitionTo(ParcelStatus target) {
        Set<ParcelStatus> allowed = ALLOWED_TRANSITIONS.get(this);
        return allowed != null && allowed.contains(target);
    }

    public Set<ParcelStatus> getAllowedTransitions() {
        return ALLOWED_TRANSITIONS.getOrDefault(this, Set.of());
    }

    public boolean isTerminal() {
        return this == DELIVERED || this == RETURNED || this == CANCELLED;
    }

    public boolean isDeletable() {
        return this == CANCELLED || this == RETURNED;
    }
}
```

---

## 3. Modifications du Code Existant

### 3.1 Ajouter le Flag `deleted` à l'Entity

**Modifier `Parcel.java`:**

```java
@Document(collection = "parcels")
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@CompoundIndexes({
    // ... indexes existants
})
public class Parcel {
    // ... champs existants ...

    @Builder.Default
    private Boolean deleted = false;  // NOUVEAU: pour soft delete
}
```

### 3.2 Modifier le Repository pour Exclure les Deleted

**Modifier `ParcelRepositoryCustomImpl.java`:**

Ajouter dans la méthode `searchParcels` :

```java
// Exclure les parcels supprimés
criteria.and("deleted").ne(true);
```

**Modifier `ParcelRepository.java`:**

```java
@Repository
public interface ParcelRepository extends MongoRepository<Parcel, String>, ParcelRepositoryCustom {

    // Existant
    Parcel findByParcelId(String parcelId);

    // NOUVEAU: exclure les deleted
    Optional<Parcel> findByParcelIdAndDeletedFalse(String parcelId);

    // NOUVEAU: pour vérifier existence avant création
    boolean existsByParcelId(String parcelId);
}
```

### 3.3 Créer les Exceptions

**`exception/ParcelNotFoundException.java`:**

```java
public class ParcelNotFoundException extends RuntimeException {
    private final String parcelId;

    public ParcelNotFoundException(String parcelId) {
        super("Parcel not found: " + parcelId);
        this.parcelId = parcelId;
    }

    public String getParcelId() {
        return parcelId;
    }
}
```

**`exception/InvalidStatusTransitionException.java`:**

```java
public class InvalidStatusTransitionException extends RuntimeException {
    private final ParcelStatus currentStatus;
    private final ParcelStatus targetStatus;

    public InvalidStatusTransitionException(ParcelStatus current, ParcelStatus target) {
        super(String.format("Cannot transition from %s to %s", current, target));
        this.currentStatus = current;
        this.targetStatus = target;
    }

    public ParcelStatus getCurrentStatus() { return currentStatus; }
    public ParcelStatus getTargetStatus() { return targetStatus; }
    public Set<ParcelStatus> getAllowedTransitions() {
        return currentStatus.getAllowedTransitions();
    }
}
```

**`exception/ParcelNotDeletableException.java`:**

```java
public class ParcelNotDeletableException extends RuntimeException {
    private final String parcelId;
    private final ParcelStatus status;

    public ParcelNotDeletableException(String parcelId, ParcelStatus status) {
        super(String.format("Parcel %s with status %s cannot be deleted", parcelId, status));
        this.parcelId = parcelId;
        this.status = status;
    }

    public String getParcelId() { return parcelId; }
    public ParcelStatus getStatus() { return status; }
}
```

---

## 4. Gestion d'Erreurs - Exception Handler

### 4.1 Créer le Global Exception Handler

**`exception/GlobalExceptionHandler.java`:**

```java
@RestControllerAdvice
public class GlobalExceptionHandler extends ResponseEntityExceptionHandler {

    @ExceptionHandler(ParcelNotFoundException.class)
    public ResponseEntity<ProblemDetail> handleParcelNotFound(ParcelNotFoundException ex) {
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.NOT_FOUND,
            ex.getMessage()
        );
        problem.setTitle("Parcel Not Found");
        problem.setProperty("parcelId", ex.getParcelId());
        return ResponseEntity.status(HttpStatus.NOT_FOUND).body(problem);
    }

    @ExceptionHandler(InvalidStatusTransitionException.class)
    public ResponseEntity<ProblemDetail> handleInvalidTransition(InvalidStatusTransitionException ex) {
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.BAD_REQUEST,
            ex.getMessage()
        );
        problem.setTitle("Invalid Status Transition");
        problem.setProperty("currentStatus", ex.getCurrentStatus().name());
        problem.setProperty("targetStatus", ex.getTargetStatus().name());
        problem.setProperty("allowedTransitions",
            ex.getAllowedTransitions().stream().map(Enum::name).toList());
        return ResponseEntity.badRequest().body(problem);
    }

    @ExceptionHandler(ParcelNotDeletableException.class)
    public ResponseEntity<ProblemDetail> handleNotDeletable(ParcelNotDeletableException ex) {
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.BAD_REQUEST,
            ex.getMessage()
        );
        problem.setTitle("Parcel Not Deletable");
        problem.setProperty("parcelId", ex.getParcelId());
        problem.setProperty("currentStatus", ex.getStatus().name());
        problem.setProperty("deletableStatuses", List.of("CANCELLED", "RETURNED"));
        return ResponseEntity.badRequest().body(problem);
    }

    @ExceptionHandler(DuplicateParcelException.class)  // Existant
    public ResponseEntity<ProblemDetail> handleDuplicateParcel(DuplicateParcelException ex) {
        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.CONFLICT,
            ex.getMessage()
        );
        problem.setTitle("Duplicate Parcel");
        problem.setProperty("parcelId", ex.getParcelId());
        return ResponseEntity.status(HttpStatus.CONFLICT).body(problem);
    }

    @Override
    protected ResponseEntity<Object> handleMethodArgumentNotValid(
            MethodArgumentNotValidException ex,
            HttpHeaders headers,
            HttpStatusCode status,
            WebRequest request) {

        ProblemDetail problem = ProblemDetail.forStatusAndDetail(
            HttpStatus.BAD_REQUEST,
            "Validation failed"
        );
        problem.setTitle("Validation Error");

        Map<String, String> errors = ex.getBindingResult()
            .getFieldErrors()
            .stream()
            .collect(Collectors.toMap(
                FieldError::getField,
                fe -> fe.getDefaultMessage() != null ? fe.getDefaultMessage() : "Invalid value",
                (e1, e2) -> e1
            ));

        problem.setProperty("errors", errors);
        return ResponseEntity.badRequest().body(problem);
    }
}
```

### 4.2 Format d'Erreur RFC 7807

**Exemple de réponse d'erreur - Transition invalide:**

```json
{
  "type": "about:blank",
  "title": "Invalid Status Transition",
  "status": 400,
  "detail": "Cannot transition from DELIVERED to CANCELLED",
  "instance": "/store_parcels/parcel/P000001",
  "currentStatus": "DELIVERED",
  "targetStatus": "CANCELLED",
  "allowedTransitions": []
}
```

**Exemple de réponse d'erreur - Parcel non supprimable:**

```json
{
  "type": "about:blank",
  "title": "Parcel Not Deletable",
  "status": 400,
  "detail": "Parcel P000001 with status READY_FOR_PICKUP cannot be deleted",
  "instance": "/store_parcels/parcel/P000001",
  "parcelId": "P000001",
  "currentStatus": "READY_FOR_PICKUP",
  "deletableStatuses": ["CANCELLED", "RETURNED"]
}
```

---

## 5. Service Layer - Nouvelles Méthodes

### 5.1 Interface ParcelService

**Ajouter dans `service/ParcelService.java`:**

```java
public interface ParcelService {
    // Méthodes existantes
    Page<Parcel> searchParcels(SearchRequestDTO request);
    Parcel getParcelById(String parcelId);
    Parcel createParcel(CreateParcelRequestDTO request);

    // NOUVELLES méthodes
    Parcel updateParcel(String parcelId, UpdateParcelRequestDTO request);
    void deleteParcel(String parcelId);
}
```

### 5.2 Implémentation

**Ajouter dans `service/ParcelServiceImpl.java`:**

```java
@Override
@Transactional
@CacheEvict(value = "parcels", key = "#parcelId")
public Parcel updateParcel(String parcelId, UpdateParcelRequestDTO request) {
    Parcel parcel = parcelRepository.findByParcelIdAndDeletedFalse(parcelId)
        .orElseThrow(() -> new ParcelNotFoundException(parcelId));

    // Update status with transition validation
    if (request.parcelStatus() != null) {
        ParcelStatus newStatus = ParcelStatus.valueOf(request.parcelStatus());
        if (!parcel.getStatus().canTransitionTo(newStatus)) {
            throw new InvalidStatusTransitionException(parcel.getStatus(), newStatus);
        }
        parcel.setStatus(newStatus);
        parcel.setStatusUpdateDate(Instant.now());
    }

    // Update isPaid
    if (request.isPaid() != null) {
        parcel.setIsPaid(request.isPaid());
    }

    // Update expirationDate and recalculate isExpired
    if (request.expirationDate() != null) {
        parcel.setExpirationDate(request.expirationDate());
        parcel.setIsExpired(request.expirationDate().isBefore(Instant.now()));
    }

    // Update customer contact info
    if (request.customer() != null) {
        Customer customer = parcel.getCustomer();
        if (request.customer().email() != null) {
            customer.setEmail(request.customer().email());
        }
        if (request.customer().phoneNumber() != null) {
            customer.setPhoneNumber(request.customer().phoneNumber());
        }
    }

    // Update storage location
    if (request.storageLocation() != null && request.storageLocation().locationCode() != null) {
        parcel.getStorageLocation().setLocationCode(request.storageLocation().locationCode());
    }

    return parcelRepository.save(parcel);
}

@Override
@Transactional
@CacheEvict(value = "parcels", key = "#parcelId")
public void deleteParcel(String parcelId) {
    Parcel parcel = parcelRepository.findByParcelIdAndDeletedFalse(parcelId)
        .orElseThrow(() -> new ParcelNotFoundException(parcelId));

    if (!parcel.getStatus().isDeletable()) {
        throw new ParcelNotDeletableException(parcelId, parcel.getStatus());
    }

    parcel.setDeleted(true);
    parcelRepository.save(parcel);
}
```

---

## 6. Controller - Nouveaux Endpoints

### 6.1 Ajouter dans ParcelController

**Modifier `controller/ParcelController.java`:**

```java
@RestController
@RequestMapping("/store_parcels")
@RequiredArgsConstructor
public class ParcelController {

    private final ParcelService parcelService;
    private final ParcelMapper parcelMapper;

    // ... endpoints existants (health, search, getByParcelId, create) ...

    @PutMapping("/parcel/{parcelId}")
    public ResponseEntity<ParcelResponseDTO> updateParcel(
            @PathVariable String parcelId,
            @Valid @RequestBody UpdateParcelRequestDTO request) {

        Parcel updated = parcelService.updateParcel(parcelId, request);
        ParcelResponseDTO response = parcelMapper.toResponseDTO(updated);
        return ResponseEntity.ok(response);
    }

    @DeleteMapping("/parcel/{parcelId}")
    public ResponseEntity<Void> deleteParcel(@PathVariable String parcelId) {
        parcelService.deleteParcel(parcelId);
        return ResponseEntity.noContent().build();
    }
}
```

---

## 7. Tests Requis

### 7.1 Tests Unitaires du Service

**`test/service/ParcelServiceImplTest.java`:**

```java
@ExtendWith(MockitoExtension.class)
class ParcelServiceImplTest {

    @Mock
    private ParcelRepository parcelRepository;

    @InjectMocks
    private ParcelServiceImpl parcelService;

    @Test
    void updateParcel_shouldUpdateStatusAndTimestamp() {
        // Given
        Parcel existingParcel = createParcelWithStatus(ParcelStatus.READY_FOR_PICKUP);
        when(parcelRepository.findByParcelIdAndDeletedFalse("P000001"))
            .thenReturn(Optional.of(existingParcel));
        when(parcelRepository.save(any())).thenAnswer(i -> i.getArgument(0));

        UpdateParcelRequestDTO request = new UpdateParcelRequestDTO(
            "PICKED_UP", null, null, null, null
        );

        // When
        Parcel result = parcelService.updateParcel("P000001", request);

        // Then
        assertThat(result.getStatus()).isEqualTo(ParcelStatus.PICKED_UP);
        assertThat(result.getStatusUpdateDate()).isNotNull();
    }

    @Test
    void updateParcel_shouldRejectInvalidTransition() {
        // Given
        Parcel deliveredParcel = createParcelWithStatus(ParcelStatus.DELIVERED);
        when(parcelRepository.findByParcelIdAndDeletedFalse("P000001"))
            .thenReturn(Optional.of(deliveredParcel));

        UpdateParcelRequestDTO request = new UpdateParcelRequestDTO(
            "CANCELLED", null, null, null, null
        );

        // When/Then
        assertThrows(InvalidStatusTransitionException.class,
            () -> parcelService.updateParcel("P000001", request));
    }

    @Test
    void deleteParcel_shouldSoftDeleteCancelledParcel() {
        // Given
        Parcel cancelledParcel = createParcelWithStatus(ParcelStatus.CANCELLED);
        when(parcelRepository.findByParcelIdAndDeletedFalse("P000001"))
            .thenReturn(Optional.of(cancelledParcel));
        when(parcelRepository.save(any())).thenAnswer(i -> i.getArgument(0));

        // When
        parcelService.deleteParcel("P000001");

        // Then
        verify(parcelRepository).save(argThat(p -> p.getDeleted()));
    }

    @Test
    void deleteParcel_shouldRejectActiveParcel() {
        // Given
        Parcel activeParcel = createParcelWithStatus(ParcelStatus.READY_FOR_PICKUP);
        when(parcelRepository.findByParcelIdAndDeletedFalse("P000001"))
            .thenReturn(Optional.of(activeParcel));

        // When/Then
        assertThrows(ParcelNotDeletableException.class,
            () -> parcelService.deleteParcel("P000001"));
    }

    private Parcel createParcelWithStatus(ParcelStatus status) {
        return Parcel.builder()
            .parcelId("P000001")
            .orderId("ORD123456")
            .pickupStoreId("ST001")
            .status(status)
            .statusUpdateDate(Instant.now())
            .expirationDate(Instant.now().plus(30, ChronoUnit.DAYS))
            .isExpired(false)
            .isPaid(false)
            .deleted(false)
            .customer(Parcel.Customer.builder()
                .firstName("John")
                .lastName("Doe")
                .build())
            .storageLocation(Parcel.StorageLocation.builder()
                .storeId("ST001")
                .locationCode("RACK-A-1")
                .build())
            .build();
    }
}
```

### 7.2 Tests de la Machine d'État

**`test/model/ParcelStatusTest.java`:**

```java
class ParcelStatusTest {

    @ParameterizedTest
    @CsvSource({
        "PENDING, IN_TRANSIT, true",
        "PENDING, CANCELLED, true",
        "PENDING, DELIVERED, false",
        "IN_TRANSIT, READY_FOR_PICKUP, true",
        "IN_TRANSIT, CANCELLED, true",
        "READY_FOR_PICKUP, PICKED_UP, true",
        "READY_FOR_PICKUP, EXPIRED, true",
        "READY_FOR_PICKUP, CANCELLED, true",
        "READY_FOR_PICKUP, DELIVERED, false",
        "PICKED_UP, DELIVERED, true",
        "EXPIRED, RETURNED, true",
        "DELIVERED, CANCELLED, false",
        "RETURNED, CANCELLED, false",
        "CANCELLED, PENDING, false"
    })
    void shouldValidateStatusTransitions(ParcelStatus from, ParcelStatus to, boolean expected) {
        assertThat(from.canTransitionTo(to)).isEqualTo(expected);
    }

    @ParameterizedTest
    @EnumSource(value = ParcelStatus.class, names = {"DELIVERED", "RETURNED", "CANCELLED"})
    void terminalStatusesShouldNotHaveTransitions(ParcelStatus status) {
        assertThat(status.isTerminal()).isTrue();
        assertThat(status.getAllowedTransitions()).isEmpty();
    }

    @ParameterizedTest
    @EnumSource(value = ParcelStatus.class, names = {"CANCELLED", "RETURNED"})
    void shouldBeDeletable(ParcelStatus status) {
        assertThat(status.isDeletable()).isTrue();
    }

    @ParameterizedTest
    @EnumSource(value = ParcelStatus.class, names = {"PENDING", "IN_TRANSIT", "READY_FOR_PICKUP", "PICKED_UP", "EXPIRED", "DELIVERED"})
    void shouldNotBeDeletable(ParcelStatus status) {
        assertThat(status.isDeletable()).isFalse();
    }
}
```

### 7.3 Tests d'Intégration

**`test/controller/ParcelControllerIntegrationTest.java`:**

```java
@SpringBootTest
@AutoConfigureMockMvc
@Testcontainers
class ParcelControllerIntegrationTest {

    @Container
    static MongoDBContainer mongoDBContainer = new MongoDBContainer("mongo:6.0");

    @DynamicPropertySource
    static void setProperties(DynamicPropertyRegistry registry) {
        registry.add("spring.data.mongodb.uri", mongoDBContainer::getReplicaSetUrl);
    }

    @Autowired
    private MockMvc mockMvc;

    @Autowired
    private ParcelRepository parcelRepository;

    @BeforeEach
    void setup() {
        parcelRepository.deleteAll();
    }

    @Test
    void updateParcel_shouldReturn200AndUpdatedParcel() throws Exception {
        // Given
        Parcel parcel = createAndSaveParcel(ParcelStatus.READY_FOR_PICKUP);

        String requestBody = """
            {
                "parcelStatus": "PICKED_UP",
                "isPaid": true
            }
            """;

        // When/Then
        mockMvc.perform(put("/store_parcels/parcel/" + parcel.getParcelId())
                .contentType(MediaType.APPLICATION_JSON)
                .content(requestBody))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.parcelStatus").value("PICKED_UP"))
            .andExpect(jsonPath("$.isPaid").value(true));
    }

    @Test
    void updateParcel_invalidTransition_shouldReturn400() throws Exception {
        // Given
        Parcel parcel = createAndSaveParcel(ParcelStatus.DELIVERED);

        String requestBody = """
            {
                "parcelStatus": "CANCELLED"
            }
            """;

        // When/Then
        mockMvc.perform(put("/store_parcels/parcel/" + parcel.getParcelId())
                .contentType(MediaType.APPLICATION_JSON)
                .content(requestBody))
            .andExpect(status().isBadRequest())
            .andExpect(jsonPath("$.title").value("Invalid Status Transition"))
            .andExpect(jsonPath("$.currentStatus").value("DELIVERED"))
            .andExpect(jsonPath("$.targetStatus").value("CANCELLED"))
            .andExpect(jsonPath("$.allowedTransitions").isArray());
    }

    @Test
    void deleteParcel_cancelledParcel_shouldReturn204() throws Exception {
        // Given
        Parcel parcel = createAndSaveParcel(ParcelStatus.CANCELLED);

        // When/Then
        mockMvc.perform(delete("/store_parcels/parcel/" + parcel.getParcelId()))
            .andExpect(status().isNoContent());

        // Verify soft delete
        Parcel deleted = parcelRepository.findByParcelId(parcel.getParcelId());
        assertThat(deleted.getDeleted()).isTrue();
    }

    @Test
    void deleteParcel_activeParcel_shouldReturn400() throws Exception {
        // Given
        Parcel parcel = createAndSaveParcel(ParcelStatus.READY_FOR_PICKUP);

        // When/Then
        mockMvc.perform(delete("/store_parcels/parcel/" + parcel.getParcelId()))
            .andExpect(status().isBadRequest())
            .andExpect(jsonPath("$.title").value("Parcel Not Deletable"))
            .andExpect(jsonPath("$.deletableStatuses").isArray());
    }

    @Test
    void deleteParcel_notFound_shouldReturn404() throws Exception {
        mockMvc.perform(delete("/store_parcels/parcel/P999999"))
            .andExpect(status().isNotFound())
            .andExpect(jsonPath("$.title").value("Parcel Not Found"));
    }

    private Parcel createAndSaveParcel(ParcelStatus status) {
        Parcel parcel = Parcel.builder()
            .parcelId("P" + System.currentTimeMillis())
            .orderId("ORD123456")
            .pickupStoreId("ST001")
            .status(status)
            .statusUpdateDate(Instant.now())
            .expirationDate(Instant.now().plus(30, ChronoUnit.DAYS))
            .isExpired(false)
            .isPaid(false)
            .deleted(false)
            .customer(Parcel.Customer.builder()
                .firstName("John")
                .lastName("Doe")
                .email("john@example.com")
                .build())
            .storageLocation(Parcel.StorageLocation.builder()
                .storeId("ST001")
                .locationCode("RACK-A-1")
                .build())
            .build();
        return parcelRepository.save(parcel);
    }
}
```

---

## 8. Checkpoints Part 2

### Checkpoint 2.1: Machine d'État des Statuts

**Validation:**
```java
// Tests unitaires passent
./mvnw test -Dtest=ParcelStatusTest
```

**Success Criteria:**
- Enum `ParcelStatus` avec méthodes `canTransitionTo()`, `getAllowedTransitions()`, `isTerminal()`, `isDeletable()`
- Tests couvrent toutes les transitions

**Estimated Time:** 2-3 hours

---

### Checkpoint 2.2: Endpoint PUT Update

**Validation:**
```bash
# Test update status (READY_FOR_PICKUP → PICKED_UP)
curl -X PUT http://localhost:8080/store_parcels/parcel/P000001 \
  -H "Content-Type: application/json" \
  -d '{"parcelStatus": "PICKED_UP", "isPaid": true}'
# Expected: 200 OK

# Test invalid transition (DELIVERED → CANCELLED)
curl -X PUT http://localhost:8080/store_parcels/parcel/P000002 \
  -H "Content-Type: application/json" \
  -d '{"parcelStatus": "CANCELLED"}'
# Expected: 400 Bad Request avec allowedTransitions
```

**Success Criteria:**
- PUT `/store_parcels/parcel/{id}` fonctionne
- Machine d'état validée
- `parcelStatusUpdateDate` mis à jour automatiquement

**Estimated Time:** 4-5 hours

---

### Checkpoint 2.3: Endpoint DELETE (Soft Delete)

**Validation:**
```bash
# Delete active parcel (should fail)
curl -X DELETE http://localhost:8080/store_parcels/parcel/P000001
# Expected: 400 Bad Request

# Delete cancelled parcel (should work)
curl -X DELETE http://localhost:8080/store_parcels/parcel/P000003
# Expected: 204 No Content

# Search should not return deleted parcel
curl -X POST http://localhost:8080/store_parcels/search ...
```

**Success Criteria:**
- DELETE `/store_parcels/parcel/{id}` soft delete
- Seuls CANCELLED/RETURNED supprimables
- Exclus des recherches

**Estimated Time:** 3-4 hours

---

### Checkpoint 2.4: Exception Handler

**Validation:**
- Toutes les erreurs retournent format RFC 7807
- ProblemDetail avec propriétés métier

**Success Criteria:**
- GlobalExceptionHandler créé
- Format JSON conforme RFC 7807
- Propriétés contextuelles (parcelId, allowedTransitions, etc.)

**Estimated Time:** 2-3 hours

---

### Checkpoint 2.5: Tests Complets

**Validation:**
```bash
./mvnw test
./mvnw jacoco:report
# Open target/site/jacoco/index.html - Coverage > 80%
```

**Success Criteria:**
- Couverture > 80%
- Tests service layer
- Tests machine d'état
- Tests d'intégration avec Testcontainers

**Estimated Time:** 4-6 hours

---

## 9. Livrables Part 2

- [ ] `UpdateParcelRequestDTO` créé
- [ ] Machine d'état dans `ParcelStatus` enum
- [ ] Exceptions: `ParcelNotFoundException`, `InvalidStatusTransitionException`, `ParcelNotDeletableException`
- [ ] `GlobalExceptionHandler` avec format RFC 7807
- [ ] Flag `deleted` dans entity `Parcel`
- [ ] Endpoint PUT `/store_parcels/parcel/{id}`
- [ ] Endpoint DELETE `/store_parcels/parcel/{id}`
- [ ] Repository modifié pour exclure deleted
- [ ] Tests unitaires (80% coverage)
- [ ] Tests d'intégration avec Testcontainers

---

## 10. Critères d'Évaluation Part 2

| Critère | Poids |
|---------|-------|
| Machine d'état et transitions | 25% |
| Endpoints PUT et DELETE | 25% |
| Gestion d'erreurs RFC 7807 | 20% |
| Tests unitaires | 15% |
| Tests d'intégration | 10% |
| Qualité du code | 5% |

---

## 11. Ressources

- [Spring Boot Validation](https://docs.spring.io/spring-boot/docs/current/reference/html/io.html#io.validation)
- [RFC 7807 - Problem Details](https://www.rfc-editor.org/rfc/rfc7807)
- [ProblemDetail in Spring 6](https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-ann-rest-exceptions.html)
- [Testcontainers MongoDB](https://java.testcontainers.org/modules/databases/mongodb/)
- [State Machine Pattern](https://refactoring.guru/design-patterns/state)
